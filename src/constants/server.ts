// paths
import path from 'path';

export const PATH_DIR_POST = path.join(process.cwd(), '_posts');
export const PATH_FILE_SERIES = path.join(process.cwd(), '_series.yml');

export const MARKDOWNS_PATH = path.join(process.cwd(), '_markdowns');

export const SQLITE_ROOT = path.join(process.cwd(), 'db.sqlite');